API key for virustotal to see if the file is malicious -> pip install vt-py to link api key
Identifier - To match and compare with current ciphers -> then ask user if they want to decode the specific cipher
Update logo ? 
Add more arguements ? 
Update ReadMe once code is updated


